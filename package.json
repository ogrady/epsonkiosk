{
  "dependencies": {
    "@types/express": "^4.17.11",
    "express": "^4.17.1",
    "pug": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  },
  "name": "epsonkiosk",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx ts-node src/index.ts",
    "watch": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'clear && npx ts-node' src/index.ts"
  },
  "author": "Daniel O'Grady",
  "license": "ISC",
  "description": "Kiosk for controlling epson scanners.",
  "devDependencies": {
    "@types/node": "^14.14.31"
  }
}
